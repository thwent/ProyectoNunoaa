import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'cl.went.duoc',
  appName: 'proyecto-nunoa',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
